#include <stdio.h>
#include <stdlib.h>

struct process{
	int bust;
	int ari;
	int avl;
	int id;
	int r_bust;
	int p_ari;
	int turn;
};

int * n;
int * time_q1;
int * time_q2;

void * multi_level_queue(void * arg)
{
	for(int i=0;i<*n;i++)
	{
		for(int j=0;j<*n;j++)
		{
			if(p[i].ari<p[j].ar
			}
		}
	}
}

int main()
{
	n = malloc(sizeof(int));
	printf("ENTER NUMBER OF PROCESS : ");
	scanf("%d",n);
	struct process p = malloc(sizeof(struct process)*n);
	for(int i=0;i<*n;i++)
	{
		int b,a;
		printf("ENTER PROCESS %d BUST TIME AND ARIVAL TIME : ");
		scanf("%d%d",b,a);
		p[i].bust = b;
		p[i].ari = a;
		p[i].avl = 0;
		p[i].id = i;
		p[i].r_bust = b;
		p[i].p_ari = 0;
		p[i].turn=0;
	}
	printf("ENTER TIME QUANTUM OF QUEUE 1 : ");
	scanf("%d",time_q1);
	printf("ENTER TIME QUANTUM OF QUEUE 2 : ");
	scanf("%d",time_q2);
}
